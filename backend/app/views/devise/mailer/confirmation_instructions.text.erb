<%= @resource.name.present? ? "#{@resource.name}様" : "こんにちは" %>

Today's Cocktailへようこそ！

アカウントのご登録ありがとうございます。
以下のURLをクリックして、メールアドレスの確認を完了してください：

<%= "#{ENV['FRONTEND_URL'] || 'http://localhost:5173'}/confirmation?confirmation_token=#{@resource.confirmation_token}" %>

このメールに心当たりがない場合は、無視していただいて問題ありません。

---
© 2025 Today's Cocktail. All rights reserved.
