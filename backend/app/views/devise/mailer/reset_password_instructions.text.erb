<% reset_password_url = "#{ENV['FRONTEND_URL'] || 'http://localhost:5173'}/reset-password?token=#{@token}" %>
<%= @resource.name.presence || @resource.email %> 様

パスワード再設定のリクエストを受け付けました。次のURLにアクセスし、24時間以内にパスワードを再設定してください。

<%= reset_password_url %>

このメールに心当たりがない場合は破棄してください。
